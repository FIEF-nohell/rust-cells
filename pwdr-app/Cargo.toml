[package]
name = "pwdr-app"
version = "0.4.1"
edition = "2021"

[[bin]]
name = "rust-cells"
path = "src/main.rs"

[dependencies]
pwdr-core = { path = "../pwdr-core" }
macroquad = "0.4"
rfd = "0.15"

[target.'cfg(windows)'.build-dependencies]
winresource = "0.1"
