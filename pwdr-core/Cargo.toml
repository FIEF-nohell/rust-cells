[package]
name = "pwdr-core"
version = "0.3.2"
edition = "2021"

[dependencies]
rayon = "1"

[dev-dependencies]
proptest = "1"
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "sim_bench"
harness = false
